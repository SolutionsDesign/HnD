@model SD.HnD.Gui.Models.MyThreadsData
@if(Model.NumberOfPages>1)
{
	for(int i = 1; i <= Model.NumberOfPages; i++)
	{
		if(Model.PageNo == i)
		{
	<span>@i</span>
		}
		else
		{
	<a href="~/Account/Threads/@i">@i</a>
		}
		if(i < Model.NumberOfPages)
		{
	@Html.Raw("&nbsp;/&nbsp;")		
		}
	}
}